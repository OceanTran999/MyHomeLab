This lab returns 2 columns, first abusing SQL Injection of the category of products using `UNION` to retrieve username and password. Finding `Administrator` credentials and log in to solve

Same as `Retrieving multiple values in a single column` challenge, but because we retrieve both `username` and `password` value **in a column**, so we need to add more value to contain 2 values for our query.
